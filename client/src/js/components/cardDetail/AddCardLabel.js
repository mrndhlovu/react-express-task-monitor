import React from "react";
import DropdownButton from "../sharedComponents/DropdownButton";

const AddCardLabel = () => {
  return (
    <DropdownButton icon="tags" buttonText="Labels">
      AddCardLabel
    </DropdownButton>
  );
};

export default AddCardLabel;
