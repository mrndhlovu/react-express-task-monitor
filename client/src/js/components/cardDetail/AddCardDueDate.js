import React from "react";
import DropdownButton from "../sharedComponents/DropdownButton";

const AddCardDueDate = () => {
  return (
    <DropdownButton icon="clock" buttonText="Add Due Date">
      AddCardDueDate
    </DropdownButton>
  );
};

export default AddCardDueDate;
