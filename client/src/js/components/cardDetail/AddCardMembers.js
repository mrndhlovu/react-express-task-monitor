import React from "react";
import DropdownButton from "../sharedComponents/DropdownButton";

const AddCardMembers = () => {
  return (
    <DropdownButton icon="users" buttonText="Members">
      AddCardMembers
    </DropdownButton>
  );
};

export default AddCardMembers;
