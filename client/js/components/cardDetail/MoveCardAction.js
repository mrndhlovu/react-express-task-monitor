import React from "react";
import DropdownButton from "../sharedComponents/DropdownButton";

const MoveCardAction = () => {
  return (
    <DropdownButton icon="right arrow" buttonText="Move">
      MoveCardAction
    </DropdownButton>
  );
};

export default MoveCardAction;
